services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      # NEXT_PUBLIC_* must reach the build — Next inlines them at build time.
      args:
        NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN: ${NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN:-}
        NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST:-}
    image: mariusbc
    container_name: mariusbc
    restart: unless-stopped
    # No host port publishing — a reverse proxy (Traefik/Dokploy) routes to the
    # container over the Docker network on port 3000.
    expose:
      - "3000"
    environment:
      # Required — generate with: openssl rand -base64 32
      AUTH_SECRET: ${AUTH_SECRET:?set AUTH_SECRET}
      # Admin password (REQUIRED) — base64 of the bcrypt hash.
      # Generate with: npm run gen:password "yourpassword"
      ADMIN_PASSWORD_HASH_B64: ${ADMIN_PASSWORD_HASH_B64:?set ADMIN_PASSWORD_HASH_B64}
      # Editable site config, persisted on the named volume below
      CONFIG_PATH: /app/data/config.json
      # Optional — PostHog analytics (production)
      NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN: ${NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN:-}
      NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST:-}
    volumes:
      # Persists config.json across redeploys
      - config-data:/app/data

volumes:
  config-data:
